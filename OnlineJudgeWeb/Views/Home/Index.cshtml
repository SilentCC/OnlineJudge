@{
    ViewData["Title"] = "Home Page";
}

<h1 class="display-4">题库列表</h1>

<br/>
<div id="problemList">
    
</div>


<script type="text/javascript">

    window.onload = function() {

        $.ajax({
            type: 'get',
            url: "http://47.240.2.193:4399/api/Problems",
            xhrFields: {
                withCredentials: true
            },
            crossDomain: true,
            success: function(data) {

                $.each(data,
                    function(i, term) {

                        var html = "<div class='card shadow-sm'>";
                        html += "<div class='card-body'>";
                        html += "<a href='Problems/Detail?problemId="+term.problem.problemId+"'>" + term.problem.problemId + " . " + term.problem.title + "</a>";
                        if (term) {
                            html += "<ion-icon name='checkmark'></ion-icon>";
                            
                        }
                        html += "</div></div><br />";
                        $("#problemList").append(html);
                    });

            },
            error: function(data) {
                console.log(data);
            }
        });
    }

</script>